<div class="container">

  <%= form_with model: @user, url:user_path(@user) do |f| %>
    <div class="mb-3">
      <h6>ハンドルネーム</h6>
      <%= f.text_field :handle_name, class: "form-control" %>
    </div>

    <div class="mb-3">
      <%= f.submit "書き換える", class: "btn btn-success" %>
    </div>
  
  <% end %>

</div>